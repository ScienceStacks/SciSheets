Serve page from Django

0.1 Generalize formula to include statements
  (a) test Column._makeStatementFromFormula
  (c) Findal all formula evaluations (evaluate, export) and
      set Formula. Use makeStatementFromFormula to verify, get stmt

1. Export
   Server
     -plumb through and test in scisheets_views
   Client side
     -inputs for file_name, inputs, outputs


1.2 Iterate
 
2.1 DEPENDENCIES
    Python math, pandas, seaborn

2. Formula

Fixes:

New feature

4. Export to python - 4 files: function.py, test_function.py, 
   input.dat, output.dat. test_function runs function.py with
