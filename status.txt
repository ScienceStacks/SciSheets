Serve page from Django

  c) Delete extraneous codes in util.py

1. Create API sufficient to do TruthTable use case with trinary logic
  c. create S. object and change column in try/except codes generated
     import scisheets.core.api as api
     add statement "S = api.FormulasAPI(self._table)"
     [For this work to work standalone, have to run from mysite?]
  d. demo example [show column creation. First comment out formulas. Then remove comments and run.]
     S.createTruthTable(['A', 'B'])
     Ap = S.trinary(A)
     Bp = S.trinary(B)
     Ap & Bp | -Bp



2.1 DEPENDENCIES
    Python math, pandas, seaborn

2. Formula

Fixes:

New feature

4. Export to python - 4 files: function.py, test_function.py, 
   input.dat, output.dat. test_function runs function.py with
