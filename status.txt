Serve page from Django

1. Column Formula
   Client side
     -Don't allow updates to cells in a column with a non-None formula

2. Import user functions in the direction SETTINGS.SCITABLE_USER_DIRECTORY

3. Table export to SETTINGS.SCITABLE_USER_DIRECTORY
      

1.2 Iterate
 
2.1 DEPENDENCIES
    Python math, pandas, seaborn

2. Formula

Fixes:

New feature

4. Export to python - 4 files: function.py, test_function.py, 
   input.dat, output.dat. test_function runs function.py with
