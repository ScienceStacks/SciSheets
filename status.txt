Serve page from Django

1. Column Formula
   Server side:
     -Handle multiple formulas by iteration
     -Check handling of None values
   Client side
     -Set formula, don't allow updates to cells in formula columns,
      show formula when do a formula edit
      

1.1 Spreadsheet export

1.2 Iterate
 
2.1 DEPENDENCIES
    Python math, pandas, seaborn

2. Formula

Fixes:

New feature

4. Export to python - 4 files: function.py, test_function.py, 
   input.dat, output.dat. test_function runs function.py with
